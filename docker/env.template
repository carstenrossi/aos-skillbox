# Skillbox Docker Environment Configuration
# Kopiere diese Datei zu .env und fülle die Werte aus

# Backend JWT Configuration (für User-Authentifizierung)
JWT_SECRET=your-super-secure-backend-jwt-secret-here

# Docker Registry (für GitHub Container Registry)
GITHUB_TOKEN=your-github-personal-access-token
GITHUB_USERNAME=carsten

# Hinweis: Assistant-spezifische JWT Tokens werden über das Admin-Panel
# direkt in der Datenbank pro Assistant konfiguriert 